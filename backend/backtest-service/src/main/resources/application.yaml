server:
  port: 8086

spring:
  application:
    name: backtest-service

services:
  stock-data-url: http://localhost:8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
