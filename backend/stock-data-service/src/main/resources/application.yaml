server:
  port: 8081

spring:
  application:
    name: stock-data-service

  datasource:
    url: jdbc:postgresql://localhost:5432/stock_market_badshah
    username: postgres
    password: 9981945154aA$

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
